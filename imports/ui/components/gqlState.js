import React from "react";

export const Error = () => (
  <div className="gql-error">There is an error with the query</div>
);

export const Fetching = () => (
  <div className="gql-fetching">Fetching Your Location Data . . .</div>
);
